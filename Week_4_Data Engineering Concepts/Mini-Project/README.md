Built a complete Azure Data Factory pipeline that reads a CSV file from Azure Blob Storage, validates the file using the Get Metadata activity, copies the data to a new destination container, and successfully completes the data transfer. 
Verified the successful pipeline execution, destination file creation, and metadata validation.
